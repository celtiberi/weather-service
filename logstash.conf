input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  # Add any log parsing or filtering rules here
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "weather-service-%{+YYYY.MM.dd}"
  }
}